{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run server (debug)",
            "type": "debugpy",
            "request": "launch",
            "module": "run",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "python": "${workspaceFolder}/venv/bin/python"
        }
    ]
}